{
  "name": "sakura-mc-launcher-desktop",
  "version": "0.1.0",
  "description": "樱花MC启动器 - 桌面版 (Electron)",
  "main": "main.js",
  "author": "SakuraMC",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/qw2548er/SakuraMC-Launcher.git"
  },
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --publish never",
    "dist:win": "electron-builder --win --x64 --publish never",
    "dist:mac": "electron-builder --mac --publish never",
    "dist:linux": "electron-builder --linux --publish never"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.sakuramc.launcher",
    "productName": "樱花MC启动器",
    "directories": {
      "output": "release"
    },
    "files": [
      "main.js",
      "preload.js",
      "web/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "artifactName": "SakuraMC-Launcher-${version}-${arch}-portable.${ext}"
    },
    "nsis": {
      "artifactName": "SakuraMC-Launcher-${version}-setup.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "樱花MC启动器"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Game"
    }
  }
}
